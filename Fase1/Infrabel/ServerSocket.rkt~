#lang racket
(require racket/tcp)

(define void '())

(define input-in 



;(define lstr (tcp-listen 8001 4 #f "127.0.0.1"))
;(define IN '())
;(define OUT '())
;(let-values ([(x y) (tcp-accept lstr)])
 ; (set! IN x)
  ;(set! OUT y))
;(define ctr 0)
;(define (loop)
;  (when (< ctr 50000)
    
 ;   (display (read IN))
  ;  (set! ctr (+ ctr))
   ; (loop)))

;(loop)
;(when (tcp-accept-ready? lstr)
 ; (display "ready"))

;(tcp-close lstr)
;(tcp-addresses lstr)